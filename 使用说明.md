# 使用说明

## 安装与卸载

将发布包解压到任意目录，双击 `安装.bat`。脚本会安装 Excel 差异/合并配置和三个右键入口：

- Excel 文件右键：多分支 SVN 提交
- 文件夹右键：多分支 SVN 提交
- 文件夹空白处右键：多分支 SVN 提交

部署工具或静默安装可执行：

```bat
安装.bat /quiet
安装.bat /context-only /quiet
```

卸载：

```bat
卸载.bat /quiet
卸载.bat /context-only /quiet
```

完整卸载会根据 `%LOCALAPPDATA%\\SowMergeTool\\install-state.json` 恢复安装前的 TortoiseSVN 配置。若发现配置已被用户或其他程序修改，脚本会跳过该项并以中文提示，不会覆盖外部改动。

## Excel 对比与合并

1. 选择两个或三个 Excel 文件启动工具。
2. 在工作表和差异导航中查看单元格、行、列及公式差异。
3. 使用“仅差异”筛选快速定位变更，双击差异块可跳转。
4. 按需选择源侧、目标侧或手工合并；保存前工具会检查编码、公式缓存和工作簿结构。
5. 另存为新文件并重新打开核对结果，再交给 SVN 提交。

工具不会修改原始文件的 SVN 历史，也不会自动提交工作副本。

## 多分支 SVN 提交

1. 在源分支工作副本中右键 Excel 文件、文件夹或空白处，选择“多分支 SVN 提交”。
2. 工具递归扫描当前范围，列出同一仓库、同一布局的目标分支和文件状态。
3. 搜索并勾选目标分支；源分支只用于产生候选变更，`master` 等受保护分支是否可选由工作台状态决定。
4. 在变更表中勾选要提交的 `.xlsx/.xlsm`；默认勾选已版本化的修改、新增和删除，未版本化文件需明确选择。
5. 填写提交说明，必要时从 TortoiseSVN 最近消息中选择。预检查会显示“目标分支 × 文件”的动作矩阵和阻断原因。
6. 点击“开始提交”。工具按源分支、目标分支逐步打开 TortoiseSVN 原生提交窗口，并在每个窗口关闭后重新核对实际 SVN 状态。

跨分支提交不是原子事务。任何冲突、取消、网络异常或部分文件提交都会停止后续传播，并保留批次记录。下次启动时可查看、继续或恢复未完成批次；工具不会自动回滚已经进入 SVN 仓库的版本。

## 状态说明

- 灰色项目：原有改动、未确认状态或不支持的结构，不会自动进入批次。
- 冲突、obstructed、replaced、switched、属性修改、外部项目和锁定文件：必须先在 TortoiseSVN 中处理。
- 新增文件：目标路径必须不存在且父目录已版本化，提交窗口中需要勾选“加入版本控制”。
- 删除文件：只有目标仍等于删除前基线时才允许执行，工具会先保存目标副本。
- 重命名：v1 默认阻断，请使用 TortoiseSVN 的“Repair Move”保留历史。
- 状态扫描失败、输出损坏或无法确认 pristine：整批阻断，不以时间戳猜测状态。

## 日志、批次与恢复

批次日志保存在工具数据目录，目标安装目录的 `log/` 和 `backups/` 会在升级时保留。修改目标文件前工具会记录候选哈希并保存原文件；取消或崩溃后，只恢复仍与候选完全一致的未提交内容，无法确认时保留现场并标记为未知。

## 常见问题

### 右键菜单没有出现

重新双击 `安装.bat`，或执行 `安装.bat /context-only /quiet` 后重启资源管理器。确认 `sow_merge_tool.exe` 与脚本位于同一目录。

### 为什么预检查不允许提交

通常是目标工作副本有本地改动、SVN 状态扫描失败、仓库 UUID/路径布局不一致，或源文件在预检查后发生了变化。先处理提示，再点击“刷新”重新扫描。

### TortoiseSVN 窗口关闭后没有继续下一个分支

工具检测到取消、部分文件提交或提交结果无法确认。请查看批次详情和 SVN 日志，确认已提交文件后再选择“继续”；不要直接重复复制文件。

### 如何查看历史提交说明

在提交说明区点击“Recent messages”或“Show log”。消息按当前仓库 UUID 读取，批次提交会自动追加 `[MultiBranchSync]` 标记和源分支版本。
